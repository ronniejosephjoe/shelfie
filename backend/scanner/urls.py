from django.urls import path

app_name = "scanner"

urlpatterns = [
    # Filled in properly once the scan pipeline exists (models, services,
    # views). Placeholder so the project's URL conf resolves during
    # earlier commits.
]
