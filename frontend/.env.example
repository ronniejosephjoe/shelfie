# Copy to .env and point this at whatever machine runs the Django
# backend, reachable from wherever you're running the Expo app --
# see src/config.js for the localhost-vs-LAN-IP distinction.
EXPO_PUBLIC_API_BASE_URL=http://localhost:8000
